# Noren landing page

The standalone Vue 3 landing page for Noren, built with Bun and Vite.

```sh
bun install
bun run dev
bun run build
```

The production build deploys to the Cloudflare Pages project `noren`:

```sh
bun run deploy
```
